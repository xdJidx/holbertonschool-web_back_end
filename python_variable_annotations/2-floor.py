#!/usr/bin/env python3
"""
Basic annotations - floor
"""
import math


def floor(n: float) -> int:
    """floor

    Args:
        n (float) : float to become floor

    Returns:
        int : the floor of n
    """
    return math.floor(n)
